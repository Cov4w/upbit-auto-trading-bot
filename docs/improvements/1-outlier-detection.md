# 1. Outlier Detection (이상값 제거)

## 📋 개요
- **우선순위:** 1위
- **효과:** ⭐⭐⭐⭐⭐
- **난이도:** 쉬움
- **소요 시간:** 30분
- **예상 정확도 향상:** 47% → 55~60%

## 🎯 목적
학습 데이터에서 **노이즈(이상값)**를 제거하여 AI 모델의 정확도를 높입니다.

## ❌ 현재 문제점
```python
# 모든 데이터를 그대로 학습
features = [RSI=35, MACD=-12, BB=0.18, ...]
→ 노이즈 데이터까지 학습
→ 정확도 47% (낮음)
```

**문제 케이스:**
- 플래시 크래시 시점 데이터 (비정상적 변동)
- API 에러로 인한 잘못된 데이터
- 시장 조작 의심 거래 데이터

## ✅ 해결 방법

### Isolation Forest 알고리즘 사용
```python
from sklearn.ensemble import IsolationForest

# 이상값 탐지기 생성
outlier_detector = IsolationForest(
    contamination=0.1,  # 데이터의 10%를 이상값으로 간주
    random_state=42
)

# 이상값 감지
is_outlier = outlier_detector.fit_predict(features)
# -1: 이상값, 1: 정상값

# 정상 데이터만 필터링
clean_features = features[is_outlier == 1]
clean_labels = labels[is_outlier == 1]
```

## 📊 적용 전후 비교

### Before
```
전체 데이터: 80건
이상값: 8건 (포함)
정상 데이터: 72건
학습 정확도: 47%
```

### After
```
전체 데이터: 80건
이상값: 8건 (제거) ✂️
정상 데이터: 72건만 학습
학습 정확도: 55~60% (예상)
```

## 🔧 구현 위치
- **파일:** `data_manager.py`
- **클래스:** `ModelLearner`
- **메서드:** `train_initial_model()` 내부

## 📈 예상 효과
1. **노이즈 제거:** 품질 낮은 데이터 10% 제거
2. **정확도 향상:** 8~13% 포인트 상승
3. **과적합 방지:** 일반화 성능 향상
4. **안정성:** 모델 예측 일관성 향상

## 💡 주의사항
- `contamination` 값은 0.05~0.15 사이 권장
- 너무 높으면 정상 데이터까지 제거될 수 있음
- 데이터가 30건 미만일 때는 비활성화 권장

## 🔗 참고 자료
- Freqtrade: https://www.freqtrade.io/en/stable/freqai-feature-engineering/#outlier-detection
- Scikit-learn Isolation Forest: https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.IsolationForest.html
